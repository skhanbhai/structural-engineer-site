node_modules/
.wrangler/
.DS_Store
*.log
# Internal working docs — repo is public, keep these local only
docs/
# Draft asset with fabricated job details — do not publish
assets/services/panoptic-beam-calculation-example.svg
# Search Console exports — business data, repo is public and wrangler
# serves the repo root, so keep these out of git AND the asset bucket.
GSC Console/
